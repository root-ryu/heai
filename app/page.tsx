"use client";

import { useState, useRef, useEffect } from 'react';
import { Send, Minimize2 } from 'lucide-react';

import MainPage from '@/app/src/components/MainPage'; // 경로에 맞게 수정

export default function Page() {
  return <MainPage />;
}